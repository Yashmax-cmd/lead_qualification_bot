# Root Level Environment Variables for Docker Compose
# Copy this file to .env and update values as needed

# FastAPI Backend Configuration
FASTAPI_PORT=8000

# Streamlit Frontend Configuration  
STREAMLIT_PORT=8501

# Note: For GitHub Codespaces, Docker Compose will handle port forwarding automatically
# For custom local ports, update these values and run: docker-compose up --build
